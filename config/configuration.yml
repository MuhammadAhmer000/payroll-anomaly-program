# update this name to be more informative
# note: whenever you change the file structure, this requires change as well, add this to the README.md
# TODO: add a tool to add sections to the YAML file...

# can be "database", "excel"
ingestion_method: "excel"

file_path:
  input: "data/input/Sample_Salary_Register.xlsx"
  output: "data/output/output_data.xlsx"
  column_schema: "data/other/column_schema.xlsx"

rule_threshold:
  PF: 0.1
  HRA: 0.05
  Net_Payable: 0.01

rate_data:
  PF: 0.12


